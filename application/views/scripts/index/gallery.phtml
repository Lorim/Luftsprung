<div class="row lightbox">
    <h1 class="text-center"><?php echo $this->label;?></h1>
    <?php
    foreach ($this->entry as $entry):
        $url = $this->url(array(
            'controller' => 'index',
            'action' => 'gallery',
            'id' => $entry->getId()
                ), 'gallery');
        $title = $entry->getTitle()."<br>".$entry->getCreated();
        ?>
        <div class="col-md-3 col-sm-3 col-xs-12 image">
            <a href="<?php echo $url; ?>">
                <img src="/pictures/beitragsbild-480x640.png" class="boxed" data-pin-no-hover="true">
                <div class="overlay"><?php echo $title; ?></div>
            </a>
        </div>
    <?php endforeach; ?>
</div>
